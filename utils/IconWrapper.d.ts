import React from "react";
type TProps = {
    Icon: React.ComponentType<object>;
    iconKey: string;
    isSearchedFor: boolean;
};
declare const IconWrapper: React.FC<TProps>;
export { IconWrapper };
